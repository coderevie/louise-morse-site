# Source scans zip file (not committed; PDFs in pdfs/ folder are)
*.zip

# Note: PDFs are now committed to pdfs/ folder for linking from pages
# The original scans in Wastena Retreat/ folder remain as local backups

# Secrets and credentials
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
secrets/
credentials/

# Local tooling
.claude/
__pycache__/
*.pyc
.DS_Store
review/*.png
review/*.jpg

# The original 24-page batches; superseded by the per-discourse splits
# and wastena-full.pdf, and still held outside the repo.
pdfs/wastena/P[0-9]*.pdf

# The review pack is generated; the .docx and its strips are not sources
review/
